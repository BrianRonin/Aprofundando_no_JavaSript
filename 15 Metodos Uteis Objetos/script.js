/*

Object.keys(objeto)
retorna um array com as CHAVES do objeto
Object.values(objeto)
retorna um array com os VALORES do objeto
Object.entries(objeto)
retorna um array pra cada CHAVE & VALOR do objeto
Object.assign({}, objeto) = copia o objeto, para o objeto vazio
                                    igual o (...objeto) spread

Object.freeze(objeto)
congela o objeto não sofrendo mais alterações

Object.defineProperties(Objeto)
define propriedades de chaves do obj OBS: aula13
Object.defineProperty(Objeto)
define propriedades de uma chave do obj OBS: aula13

Object.getOwnPropertyDescriptor(objeto, valor)
leia as propriedades de um objeto
*/
